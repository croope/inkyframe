# Core — always required
Pillow>=10.0.0
inky[rpi]>=1.3.0

# Optional — install for the features you want:

# HEIC / HEIF support (uncomment register_heif_opener() in main.py too)
# pillow-heif>=0.13.0

# Face detection (enables FACE_DETECTION in config.py)
# opencv-python>=4.8.0

# Offline GPS reverse-geocoding — fast, no internet needed at display time
# reverse_geocoder>=1.5.1

# Paper texture (enables PAPER_TEXTURE in config.py)
# numpy>=1.24.0
